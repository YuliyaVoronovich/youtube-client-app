import { CardShadowColorDirective } from './card-shadow-color.directive';

describe('CardShadowColorDirective', () => {
  it('should create an instance', () => {
    const directive = new CardShadowColorDirective();
    expect(directive).toBeTruthy();
  });
});
